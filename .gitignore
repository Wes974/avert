# XcodeGen output — regenerate with `xcodegen generate`
*.xcodeproj

# Build artifacts
.build/
DerivedData/

# Never in iCloud
node_modules/

# macOS noise
.DS_Store
._*

# Test fixture: a brand logo copied locally to exercise the perceptual-hash
# signal. Never committed — a public repo has no need to redistribute brand
# assets. See Tests/pages/logo-copy/index.html.
Tests/pages/logo-copy/logo.png
